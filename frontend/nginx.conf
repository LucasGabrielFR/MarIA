server {
    listen 80;
    
    location /admin {
        alias /usr/share/nginx/html/admin;
        try_files $uri $uri/ /admin/index.html;
    }

    # Health check or redirect
    location / {
        return 301 /admin/;
    }
}
